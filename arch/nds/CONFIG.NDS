#!/bin/bash

./config.sh --platform nds --prefix $DEVKITARM --optimize-size \
            --disable-editor --disable-helpsys --disable-utils \
            --disable-libpng --enable-release --enable-meter \
            --disable-screenshots --enable-stdio-redirect "$@"
