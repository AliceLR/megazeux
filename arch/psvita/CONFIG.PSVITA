#!/bin/bash

[[ -z $VITASDK ]] && { echo "\$VITASDK is unset. Aborting"; exit 1; }

./config.sh --platform psvita --prefix $VITASDK --optimize-size \
            --disable-helpsys --disable-utils --enable-release \
            --enable-meter --disable-gl --disable-libpng -- "$@"
